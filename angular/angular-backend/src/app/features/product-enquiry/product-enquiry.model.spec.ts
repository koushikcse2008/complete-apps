import { ProductEnquiryModel } from './product-enquiry.model';

describe('ProductEnquiryModel', () => {
  it('should create an instance', () => {
    expect(new ProductEnquiryModel()).toBeTruthy();
  });
});
